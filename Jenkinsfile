pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building static project...'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing static files...'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying static site...'
            }
        }
    }
}
